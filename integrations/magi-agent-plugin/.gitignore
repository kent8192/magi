# Python bytecode caches (none should be committed)
__pycache__/
*.pyc

# Local runtime state should never live here, but ignore it defensively
# in case a user points MAGI_AGENT_STATE_DIR at the plugin directory.
.venv/
venv/
*.pid
*.log
